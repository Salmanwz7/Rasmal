<?xml version="1.0" encoding="utf-8"?>
<!-- Reusable Rasmal logo: green gradient square with stacked chevrons + "Rasmal" label. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="@dimen/logo_size"
        android:layout_height="@dimen/logo_size"
        android:background="@drawable/bg_logo_square">

        <ImageView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:contentDescription="@string/brand"
            android:src="@drawable/ic_logo_chevrons" />
    </FrameLayout>

    <TextView
        android:id="@+id/logo_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_12"
        android:letterSpacing="0.02"
        android:text="@string/brand"
        android:textColor="@color/text_primary"
        android:textSize="22sp"
        android:textStyle="bold" />
</LinearLayout>
